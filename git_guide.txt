1. create repository without README etc.
2. open git command line:
	git init
	git add .
	git commit -m 'first commit'
	git remote add origin URL
	git push -u origin master
3. git add *
   git commit -m 'later version'
   git push origin master